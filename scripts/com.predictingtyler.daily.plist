<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Predicting Tyler — daily pipeline (launchd user agent).
  Install:
    cp scripts/com.predictingtyler.daily.plist ~/Library/LaunchAgents/
    launchctl load -w ~/Library/LaunchAgents/com.predictingtyler.daily.plist
  Uninstall / pause:
    launchctl unload -w ~/Library/LaunchAgents/com.predictingtyler.daily.plist
  Run once now (to test):
    launchctl start com.predictingtyler.daily
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.predictingtyler.daily</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>/Users/humzahkhan/Projects/mr_eval/scripts/run_daily.sh</string>
  </array>

  <!-- Fires at 09:00 local, then 09:10, then every 30 min through 12:40. The
       runner does nothing once the day has already succeeded (a dated success
       marker), so these are retries, not repeated runs: if 09:00 misses because
       wifi wasn't up yet (or credits were briefly out), a later fire recovers
       the day instead of losing it. If the Mac is asleep across these times,
       launchd coalesces the missed fires into one run on wake. -->
  <key>StartCalendarInterval</key>
  <array>
    <dict><key>Hour</key><integer>9</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>9</integer><key>Minute</key><integer>10</integer></dict>
    <dict><key>Hour</key><integer>9</integer><key>Minute</key><integer>40</integer></dict>
    <dict><key>Hour</key><integer>10</integer><key>Minute</key><integer>10</integer></dict>
    <dict><key>Hour</key><integer>10</integer><key>Minute</key><integer>40</integer></dict>
    <dict><key>Hour</key><integer>11</integer><key>Minute</key><integer>10</integer></dict>
    <dict><key>Hour</key><integer>11</integer><key>Minute</key><integer>40</integer></dict>
    <dict><key>Hour</key><integer>12</integer><key>Minute</key><integer>10</integer></dict>
    <dict><key>Hour</key><integer>12</integer><key>Minute</key><integer>40</integer></dict>
  </array>

  <key>StandardOutPath</key>
  <string>/Users/humzahkhan/Projects/mr_eval/data/daily.launchd.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/humzahkhan/Projects/mr_eval/data/daily.launchd.log</string>

  <key>RunAtLoad</key>
  <false/>
</dict>
</plist>
